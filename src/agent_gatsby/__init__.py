"""Top-level package metadata for Agent Gatsby.

This module exposes the package version for the local, citation-verified
literary-analysis pipeline. The package itself is organized around explicit
pipeline stages that ingest a locked source text, derive evidence artifacts,
produce a verified English analysis, generate translations, and render final
PDF deliverables.
"""

__version__ = "0.1.0"
